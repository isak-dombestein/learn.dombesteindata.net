- Add Footer to reference pages to get text further up on screen { FIXED }

- Larger spacing between each category needed { FIXED }

- Letter capitalism is inconsistent { FIXED }

- There is no link to utilities from html/css/js pages { FIXED }

- The logos should be added to the navbar and turned into mini versions of the buttons on index. Buttons should also be highlighted based on which page we're currently on. { FIXED }